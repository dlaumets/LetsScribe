# Legacy domain — redirect to LetsScribe (remove when DNS old record is gone)
transcriber.letscore.tech {
	redir https://scribe.letscore.tech{uri} permanent
}

scribe.letscore.tech {
	encode gzip

	handle /bot/webhook* {
		reverse_proxy bot:8081
	}

	handle {
		reverse_proxy api:8000
	}
}
